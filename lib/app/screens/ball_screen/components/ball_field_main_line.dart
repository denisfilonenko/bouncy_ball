import 'package:flutter/material.dart';

class BallFieldMainLine extends StatelessWidget {
  const BallFieldMainLine({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 3,
      thickness: 3,
      color: Colors.red,
    );
  }
}
